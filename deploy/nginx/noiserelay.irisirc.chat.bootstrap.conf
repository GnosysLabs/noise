server {
    listen 80;
    listen [::]:80;
    server_name noiserelay.irisirc.chat;
    access_log off;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 404;
    }
}
