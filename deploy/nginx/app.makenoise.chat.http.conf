server {
    listen 80;
    listen [::]:80;
    server_name app.makenoise.chat;

    root /var/www/app.makenoise.chat;
    index index.html;

    location /.well-known/acme-challenge/ {
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
