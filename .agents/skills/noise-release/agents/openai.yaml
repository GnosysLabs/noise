interface:
  display_name: "noise release"
  short_description: "Build, sign, deploy, and verify noise"
  default_prompt: "Use $noise-release to build, sign, deploy, and verify the requested noise platforms."
